# Base URL of the FastAPI backend, without a trailing slash.
#
# Only VITE_-prefixed variables reach client code, and everything so prefixed is
# PUBLIC — it ends up readable in the shipped bundle. INTERNAL_API_KEY must
# never be given a VITE_ prefix.
VITE_API_URL=http://localhost:8000
